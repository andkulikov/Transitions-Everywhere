buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:2.2.2'
    }

    project.ext {
        buildToolsVersion = "23.0.2"
        minSdkVersion = 8
        targetSdkVersion = 25

        versionName = "1.7.1"
    }
}

allprojects {
    repositories {
        jcenter()
    }
}
