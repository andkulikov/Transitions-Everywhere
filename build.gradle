buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:1.5.0'
    }

    project.ext {
        buildToolsVersion = "23.0.2"
        minSdkVersion = 8
        targetSdkVersion = 23

        versionCode = 11
        versionName = "1.6.1"
    }
}

allprojects {
    repositories {
        jcenter()
    }
}
