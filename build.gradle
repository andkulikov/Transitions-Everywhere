buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:2.2.2'
    }

    project.ext {
        buildToolsVersion = "25.0.0"
        minSdkVersion = 14
        targetSdkVersion = 23

        versionCode = 19
        versionName = "1.7.0"
    }
}

allprojects {
    repositories {
        jcenter()
    }
}
