buildscript {
    repositories {
        jcenter()
        google()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.2.1'
    }

    project.ext {
        buildToolsVersion = "28.0.3"
        minSdkVersion = 9
        targetSdkVersion = 28

        versionName = "1.8.0"
    }
}

allprojects {
    repositories {
        jcenter()
        google()
    }
}
